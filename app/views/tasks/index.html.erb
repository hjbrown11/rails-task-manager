<h1>Tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li> <%= task.completed ? 'Done: ' : 'Not done: ' %><%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>
<%= link_to "Add a new task", new_task_path %>
